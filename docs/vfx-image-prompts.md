# GPT Image 2.5 — game VFX prompts

Use with [`vfx-generate`](../skills/assets/vfx-generate/SKILL.md). Swap `{style}` for the project art bible. Background `#00FF00` if you will key; ask transparent when the model keeps alpha.

## hit-spark / light

```
Game-ready light hit-spark VFX plate, tiny yellow-white sparks bursting left from a contact point,
2-frame flipbook in one row, even spacing,
painterly anime game VFX, {style},
flat #00FF00 background, no character, no UI, no text, no watermark.
```

## impact-burst / heavy

```
Game-ready heavy impact-burst VFX plate, shock ring plus debris flying opposite a right-to-left slash,
4-frame flipbook in one row, first frame brightest,
{style}, flat #00FF00 background, no character, no UI, no text, no watermark.
```

## slash-arc

```
Game-ready sword slash-arc VFX plate, curved energy ribbon with a thin white highlight on the leading edge,
smoke specks following the arc, 4-frame flipbook left to right,
{style}, flat #00FF00 background, no sword model, no character, no text, no watermark.
```

## speed-line

```
Game-ready speed-line VFX plate, horizontal motion streaks and a faint afterimage silhouette facing right,
only high-speed language, no walking pose,
3-frame flipbook, {style}, flat #00FF00 background, no UI, no text, no watermark.
```

## muzzle-flash

```
Game-ready muzzle-flash VFX plate, short barrel bloom and two spark streaks,
1-2 frames side by side, {style},
flat #00FF00 background, no gun body, no character, no text, no watermark.
```

## trail-streak

```
Game-ready projectile trail VFX plate, tapering ribbon behind an implied bullet flying right,
3-frame flipbook, {style}, flat #00FF00 background, no full weapon, no text, no watermark.
```

## explode-burst

```
Game-ready small explosion VFX plate, fire core, smoke shell, a few sparks,
4-frame flipbook, {style}, flat #00FF00 background, no crater scene, no text, no watermark.
```

## status-aura

```
Game-ready looping burn-aura VFX plate, low ring of flame around an empty cylinder,
4-frame seamless loop, {style}, flat #00FF00 background, no character, no text, no watermark.
```

Do not paste a shipped game still as the style lock. Do not ask the model to draw hitboxes.
