# Interrupted audit recovery

## What failed, and what did not

The conversation stopped before later prepared changes became branch commits. There is no diagnostic evidence here for a particular platform failure cause. The repository was not rolled back or corrupted: the last published pre-recovery commit was `8b199985e0a89c47cd98319c3f8cc5ac4387db2b`, and its GitHub Actions run `34713332029` completed successfully.

A staged Git tree is not a branch checkpoint. Recovery used the actual remote refs and an authenticated source artifact rather than treating the interrupted write request as successful.

## Published recovery checkpoints

| Commit | Scope |
|---|---|
| `2486ede7196b6a97b862dd76d0fce8bdd0c939c1` | Integrate concurrent main `f7d178a53ff145a800b56a76b20de350ee4f792f`, preserving the nine new skills and expanded catalogs; consolidate studio selector aliases and repair its installed-pack links. |
| `5de1050b9df6f4b369d352cf28df9e229a90cd97` | Restore entry/dispatcher/router changes, fix all five README installation commands, generate the complete 144-skill path catalog, and test metadata, routing vocabulary, catalog drift and local documentation links. |

The second checkpoint has tree `882e5ac05fdb23fd44fc60c9843515f5fd9dadb9`, identical to the locally tested Git tree. Its GitHub Actions run `34715838518` completed successfully. Branch: `improve/skills-audit-2026-09-13`. No force-push and no direct write to main.

## Additional corrections in this commit

`pixel-animation` now distinguishes the attack hitbox from the vulnerable hurtbox, including independent timeline intervals and multi-tick sprite cells. `hitstun-recover` now measures advantage from both actors' first legal action ticks and includes a numeric counterexample showing that positive advantage does not prove a true combo or an infinite.

Two documentation regression checks were observed failing before these corrections, then passing afterward. These are text/example guards, not an LLM evaluation or an engine simulation. The CI static-check step also explicitly enforces catalog freshness.

## Verification scope

| Check | Result and limitation |
|---|---|
| Existing installer/checker suite at pre-recovery commit | 27 tests passed locally; remote CI also succeeded. |
| Recovery regression cases | Failed before repair; passed after repair. Covers installation, catalog completeness, explicit route vocabulary, non-README links and catalog bootstrap. |
| Complete local suite after the two semantic corrections | 34 tests passed. Includes tool tests, static document contract checks and the arithmetic example. |
| `python tools/skill_quality.py --check-catalog` | 144 skill files inspected; zero static errors under the checker's documented scope. |
| `git diff --check` | Passed for the recovery changes. |
| Agent routing across languages | Not run. Static names/mode checks do not establish model routing accuracy. |
| Engine integration, cross-device input and actual gameplay | Not run. No representative game build is present in this repository. |
| Human playability and long-duration soak | Not run. Do not infer either from these repository checks. |

## Remaining substantive audit work

The original [audit plan](../superpowers/plans/2026-09-13-skills-audit.md) is not yet complete. Recovery does not claim that all 144 skills received deep semantic refinement. The following are concrete follow-up review targets, not completed changes:

| Skills | Review target | Required evidence before closing |
|---|---|---|
| camera-anti-clip | Final shake offsets and near-plane extent can escape a previously safe sweep. | Wall/corner scene with wide FOV, aspect-ratio variation, initial overlaps and shake enabled. |
| model-pipeline, character-rig, ik-foot-locking | Canonical asset units/axes versus engine conversions; optional toe joints and contacts on moving platforms. | Import/rig fixtures for each claimed adapter; stationary and moving support tests. |
| unity, unreal, threejs, pixijs, godot, custom | Pin supported API versions, tick ownership, lifecycle and per-actor freeze behavior. | Minimal runnable adapter fixtures; do not call a render callback or actor Tick fixed-rate by assumption. |
| action-feel, hitbox-hurtbox, knockback-body | Stable per-tick hit resolution, duplicate contacts, interruption and shared ownership. | Same-tick trade/order tests, multi-hit IDs, cancellation and wall collision fixtures. |
| netcode-feel, fighting-design, ghost-line | Prediction can be corrected; snapshots, RNG and effects need explicit replay contracts. | Controlled latency/loss fixtures and deterministic state hashes where promised. |
| performance-budget, performance-optimization | CPU/GPU critical paths, frame-time percentiles and measured scope; equal simulation is not identical presentation latency. | Identical scenes and settings on declared hardware, with separate CPU/GPU measurements. |
| jump-leniency, platform-jump, locomotion | One owner for forgiveness windows and consistent tick-to-millisecond examples. | Before/at/after boundary tests at multiple render rates. |
| save-integrity, cloud-save, entitlement-grant | Atomic persistence, conflict resolution and idempotent grants, including consumable transaction retries. | Corrupt/old saves, concurrent devices, duplicate receipts and crash/retry fixtures. |
| gameplay-validation, game-planning, a11y-controls, rhythm-judge | Genre-neutral challenges, calibrated timing, explicit assist modes and evidence-based failure attribution. | Noncombat slices, declared accessibility settings and appropriately labeled input/playtest evidence. |
| game-monetization, crafting-loop, fov-comfort, knockback-launch | Historical prose references include names/paths that need normalization; the Markdown checker does not parse arbitrary backticked prose. | Resolve every intended cross-skill reference without inventing missing skills. |

## Resume safely

Fetch current main and the improvement branch; do not start again from the initial snapshot. Inspect new commits before continuing. Select one bounded row above, reproduce its failure, add the smallest correction and focused verification, then commit and push that step. Report the remote SHA and actual CI outcome. Preserve test evidence and leave unrun checks explicitly unrun.
